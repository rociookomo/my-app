{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "jsx": "react", // Ensure this is set to react
    "strict": true,
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ]
}
